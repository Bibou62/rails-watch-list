<%= @list.name %>

<ul>
<% @list.bookmarks.each do |bookmark|  %>
<li> <%= bookmark.comment %>
<%= link_to "Destroy", bookmark_path(bookmark.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure"}  %>
</li>
<% end  %>
</ul>
<%=  link_to "Add new bookmark", new_list_bookmark_path(params[:id]) %>
